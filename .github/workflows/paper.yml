name: paper

on: [push]

jobs:
  build-paper:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build paper
        uses: docker://openjournals/joss-paper:latest
        with:
          args: paper.md
